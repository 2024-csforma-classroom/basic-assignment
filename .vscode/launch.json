{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Test",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "command": "make test"
        }
    ]
}
